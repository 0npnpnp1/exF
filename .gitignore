# private launch notes
NOTES.md
CHANGELOG.md

# local scraps / originals not needed in the repo
Untitled.paint
/*.png
present vid.txt
New Text Document.txt

# private working area
prod/
prototype/
exf-promo/

# packaging output — NEVER commit the .pem, it's the extension's
# signing key (anyone holding it can forge updates)
*.pem
*.crx
*.zip
build/

# local Claude Code config (name is fixed by the tool — don't rename)
.claude/

# OS noise
Thumbs.db
.DS_Store
